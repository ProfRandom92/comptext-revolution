{
  "name": "comptext-revolution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CompText Revolution — Universal Token Compression Platform",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "benchmark": "tsc benchmark/compression-benchmarks.ts --module esnext --target es2020 --esModuleInterop && node benchmark/compression-benchmarks.js",
    "benchmark:perf": "tsc benchmark/performance-tests.ts --module esnext --target es2020 --esModuleInterop && node benchmark/performance-tests.js"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
